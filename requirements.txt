# 简化的 requirements.txt 文件
# 推荐使用 pyproject.toml 进行依赖管理
# 安装命令: pip install -e .

# 如果需要直接安装依赖项，可以使用以下命令：
# pip install -r requirements-dev.txt

# Gmail集成依赖（可选）：
# pip install -r requirements-gmail.txt

# 或者直接安装核心依赖：
playwright>=1.40.0
telethon>=1.34.0
openai>=1.0.0
google-generativeai>=0.3.0
tiktoken>=0.5.0
pyyaml>=6.0
aiofiles>=23.0.0
click>=8.1.0
rich>=13.0.0
aiohttp>=3.9.0
beautifulsoup4>=4.12.0
python-dotenv>=1.0.0
tqdm>=4.66.0
watchdog>=3.0.0
dataclasses-json>=0.6.0